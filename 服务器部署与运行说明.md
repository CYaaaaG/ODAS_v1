# ODAS 复现项目服务器部署与运行说明

本文用于在 Linux 服务器上部署和运行本项目。当前复现范围为论文中的 **Cao 实验室 Φ-OTDR 数据集**，暂不执行 Wu 现场数据实验。

项目地址：`https://github.com/CYaaaaG/ODAS_v1`

## 一、服务器基本要求

建议使用 Ubuntu 20.04/22.04、Python 3.9～3.11、NVIDIA GPU（显存建议不低于 8 GB），并预留至少 100 GB 磁盘空间。服务器应安装与 CUDA 驱动匹配的 PyTorch。项目不要求在服务器上重新采集 DAS 数据，也不要求配置 DAS 解调设备。

## 二、登录服务器

本地 Windows 终端执行：

```bash
ssh 用户名@服务器IP
```

指定端口时执行：

```bash
ssh -p 端口号 用户名@服务器IP
```

登录后确认身份和目录：

```bash
whoami
pwd
mkdir -p ~/projects
cd ~/projects
```

## 三、拉取 GitHub 项目

### SSH 方式

服务器已配置 GitHub SSH 密钥时执行：

```bash
cd ~/projects
git clone git@github.com:CYaaaaG/ODAS_v1.git
cd ODAS_v1
git status
```

第一次连接 GitHub 可能出现主机指纹提示。确认 GitHub 主机信息后输入 `yes`，随后输入服务器 SSH 密钥密码。

### HTTPS 方式

服务器没有 GitHub SSH 密钥时执行：

```bash
cd ~/projects
git clone https://github.com/CYaaaaG/ODAS_v1.git
cd ODAS_v1
```

检查远程仓库：

```bash
git remote -v
```

## 四、准备 Cao 数据集

数据下载地址和说明位于项目的 `Data/readme.txt`。原始 `.mat` 文件和大型压缩包不会提交到 GitHub，需要在服务器单独下载或上传。

目录必须为：

```text
ODAS_v1/
├── Data/
│   ├── train/
│   │   ├── 01_background/  02_dig/  03_knock/
│   │   ├── 04_water/       05_shake/ 06_walk/
│   │   └── label.txt
│   └── test/
│       ├── 01_background/  02_dig/  03_knock/
│       ├── 04_water/       05_shake/ 06_walk/
│       └── label.txt
├── configs/
├── odas/
├── train.py
├── evaluate.py
└── requirements.txt
```

如果从本地上传数据，在本地 Windows 终端执行：

```bash
scp -r D:\DAS_Project\Open_DAS\Open_DAS_codex\ODAS_v1\Data 用户名@服务器IP:~/projects/ODAS_v1/
```

检查数据：

```bash
cd ~/projects/ODAS_v1
find Data/train -name '*.mat' | wc -l
find Data/test -name '*.mat' | wc -l
head Data/train/label.txt
ls Data/train/01_background | head
```

项目还提供训练前完整性检查。该检查会逐行读取 `label.txt`，确认其中列出的每个 `.mat` 文件都存在：

```bash
python scripts/check_data.py --data-root Data
```

只有看到以下输出后才开始训练：

```text
All indexed .mat files exist.
```

如果出现 `Missing ... indexed .mat files`，说明数据压缩包没有完整解压，或上传过程中遗漏了文件。应重新解压或重新上传完整的 `Data` 目录，不要直接删除 `label.txt` 中的缺失行，因为这会改变论文数据划分。

如果 `.mat` 文件出现在 `git status` 的待提交列表中，不要执行 `git add`，因为原始数据已经被 `.gitignore` 排除。

## 五、创建 Python 虚拟环境

```bash
cd ~/projects/ODAS_v1
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip setuptools wheel
```

如果提示没有 `venv`：

```bash
sudo apt-get update
sudo apt-get install python3-venv
```

以后每次重新登录服务器，都要重新激活：

```bash
cd ~/projects/ODAS_v1
source .venv/bin/activate
```

退出环境：

```bash
deactivate
```

## 六、安装依赖

在虚拟环境中执行：

```bash
python -m pip install -r requirements.txt
```

检查 PyTorch、CUDA 和 GPU：

```bash
python -c "import torch, torchvision, scipy, sklearn; print('torch:', torch.__version__); print('cuda:', torch.cuda.is_available()); print('gpu:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU')"
```

如果 CUDA 为 `False`，程序仍可以使用 CPU 运行，但会明显变慢。服务器有 GPU 但 PyTorch 无法识别时，应根据服务器驱动重新安装匹配的 PyTorch，不要修改算法代码掩盖环境问题。

如果出现类似下面的警告：

```text
The NVIDIA driver on your system is too old
```

先查看驱动版本：

```bash
nvidia-smi
python -c "import torch; print(torch.__version__); print(torch.version.cuda); print(torch.cuda.is_available())"
```

该警告表示当前 PyTorch 所带 CUDA 运行时与服务器 NVIDIA 驱动不匹配。优先方案是让管理员升级 NVIDIA 驱动；如果不能升级，则应安装与现有驱动兼容的 PyTorch 版本。临时验证代码流程时，可以强制使用 CPU：

```bash
CUDA_VISIBLE_DEVICES="" python train.py --config configs/cao_smoke.json --mode supervised
```

CUDA 警告本身不会解释 `.mat` 文件找不到的问题；数据完整性和 CUDA 环境需要分别处理。

## 七、冒烟测试

正式训练前，复制配置并把 `epochs` 改为 `1`：

```bash
cp configs/cao_supervised.json configs/cao_smoke.json
cp configs/cao_odas.json configs/cao_odas_smoke.json
```

在两个新配置中设置：

```json
"epochs": 1
```

运行监督基线：

```bash
python train.py --config configs/cao_smoke.json --mode supervised
```

运行 ODAS 流程：

```bash
python train.py --config configs/cao_odas_smoke.json --mode odas
```

正常情况下会生成：

```text
runs/cao_smoke/last.pt
runs/cao_smoke/history.json
runs/cao_smoke/split.json
runs/cao_odas_smoke/last.pt
runs/cao_odas_smoke/history.json
runs/cao_odas_smoke/split.json
```

检查指标：

```bash
cat runs/cao_odas_smoke/history.json
python evaluate.py --checkpoint runs/cao_odas_smoke/last.pt --config configs/cao_odas_smoke.json
```

只有冒烟测试成功后，才开始正式训练。

## 八、运行监督学习基线

监督基线用于验证数据读取、`10000×12 → 32×32` 预处理、ResNet-18 和评价指标：

```bash
python train.py --config configs/cao_supervised.json --mode supervised
python evaluate.py --checkpoint runs/cao_supervised/last.pt --config configs/cao_supervised.json
```

结果保存在 `runs/cao_supervised/`。

## 九、运行 ODAS 近似复现

当前 Cao 实验协议为：已知类为 background、dig、knock；未知类为 water、shake、walk；已知类 50% 样本作为有标签数据，其余已知类样本和全部未知类样本作为无标签数据；训练阶段不读取无标签真实标签。

运行：

```bash
python train.py --config configs/cao_odas.json --mode odas
```

## 十、使用 tmux 保持训练

服务器连接可能中断，建议使用 `tmux`：

```bash
tmux new -s odas
cd ~/projects/ODAS_v1
source .venv/bin/activate
mkdir -p runs/cao_odas
python train.py --config configs/cao_odas.json --mode odas 2>&1 | tee runs/cao_odas/terminal.log
```

按 `Ctrl+B`，再按 `D`，可以退出窗口而保持训练。重新连接：

```bash
tmux attach -t odas
```

查看 GPU：

```bash
nvidia-smi
```

## 十一、查看实验结果

```bash
cat runs/cao_odas/history.json
python -c "import json; x=json.load(open('runs/cao_odas/history.json')); print(json.dumps(x[-1], indent=2, ensure_ascii=False))"
python evaluate.py --checkpoint runs/cao_odas/last.pt --config configs/cao_odas.json
```

每组实验应使用独立配置和输出目录。例如标签比例实验：

```bash
cp configs/cao_odas.json configs/cao_odas_label10.json
```

将新配置中的以下参数修改为：

```json
"labeled_fraction": 0.1,
"output_dir": "runs/cao_odas_label10"
```

不同实验必须使用不同输出目录，避免覆盖结果。

## 十二、正式实验顺序

建议按以下顺序执行：

1. 监督学习基线；
2. Cao 数据上的 ODAS 主实验；
3. 使用至少 3 个随机种子重复主实验；
4. 标签比例实验：`0.5、0.3、0.1、0.05、0.01`；
5. 模块消融实验：监督基线、加入 UCM、加入 SSM、加入 CACM、完整 ODAS；
6. 汇总均值、标准差和复现限制。

每次实验保存配置、随机种子、数据划分、训练日志、模型权重和评价结果。

## 十三、更新代码和版本

```bash
cd ~/projects/ODAS_v1
git pull --rebase origin main
git log --oneline -5
```

如果服务器端修改了代码：

```bash
git status
git add train.py evaluate.py odas configs requirements.txt README.md

git commit -m "Update server experiment code"
git push origin main
```

原始数据、虚拟环境、模型权重和训练输出已经被 `.gitignore` 排除，不要直接上传到 GitHub。

## 十四、常见问题

### 找不到 torch

```bash
which python
python -m pip show torch
python -m pip install -r requirements.txt
```

### 找不到 `.mat` 文件

确认当前目录是项目根目录：

```bash
pwd
find Data/train -name '*.mat' | head
```

### 出现 `Expected (10000,12)`

说明数据文件格式不符合 Cao 数据集要求，或文件损坏。应检查数据来源和 `.mat` 内容，不要直接修改预处理尺寸。

### 显存不足

将配置中的 `batch_size` 从 `64` 降低为 `32`、`16` 或 `8`，并在实验记录中注明。

### 训练中断

当前版本会保存最近一轮的 `last.pt`，但暂不保存优化器状态，不能保证从中断位置继续训练。重新运行时应使用新的输出目录，保留中断记录。

## 十五、部署完成标准

满足以下条件即可认为服务器部署完成：

1. 成功拉取项目；
2. Cao 数据目录结构正确；
3. `pip install -r requirements.txt` 成功；
4. 已确认 GPU 或 CPU 运行状态；
5. 监督基线完成 1 个 epoch；
6. ODAS 流程完成 1 个 epoch；
7. 生成 `last.pt`、`history.json` 和 `split.json`；
8. `evaluate.py` 能输出开放世界指标；
9. 训练日志和配置已保存；
10. Wu 数据实验保持关闭并在实验记录中说明。
